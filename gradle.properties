## For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
#Fri May 09 21:19:05 IST 2025
org.gradle.jvmargs=-Xmx1024M -Xms512m -XX:+UseParallelGC

# Reduce max parallel Kotlin compilation workers
kotlin.compiler.execution.strategy=in-process
kotlin.daemon.jvm.options=-Xmx1024m

# Optional: Avoid using configuration cache if RAM is tight
org.gradle.configuration-cache=false
